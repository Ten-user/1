Career Guidance Platform — Final

See client/ and functions/ folders. Add Firebase config to client/src/firebaseConfig.js and set your project id in .firebaserc.

Seed institutes: node functions/create_demo_data.js

Deploy: cd client && npm install && npm run build; firebase deploy --only hosting,functions
